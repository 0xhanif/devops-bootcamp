# Secret, Variable

# Secret
Masked
contoh: token

env:
  TOKEN: ${{ secrets.TOKEN }}

# Variable
Papar penuh
contoh: URL

env:
  URL: ${{ vars.URL }}

Di dalam workflows kedua-dua terletak pada bahagian env
